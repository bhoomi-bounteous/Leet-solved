class Solution { 
    int path(int i,int j,List<List<Integer>> triangle,int[][] dp){
        if(i>=triangle.size()) return (int)1e9;
        if(i==triangle.size()-1) return triangle.get(i).get(j);
        if(dp[i][j]!=1e9) return dp[i][j];
        int one=triangle.get(i).get(j)+path(i+1,j,triangle,dp);
        int two=triangle.get(i).get(j)+path(i+1,j+1,triangle,dp);
        return dp[i][j]=Math.min(one,two);
    }
    public int minimumTotal(List<List<Integer>> triangle) {
        int n=triangle.size();
        int[][] dp=new int[n][n];
        for(int i=0;i<n;i++) Arrays.fill(dp[i],(int)1e9);
        return path(0,0,triangle,dp);
    }
}
